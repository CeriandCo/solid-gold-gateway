# Three ways to own — desktop section

## Build
- Upload the six supplied card and phone-screen images through the project asset flow under `src/assets/home/`.
- Replace only the empty `#three-ways` placeholder with the specified heading, comparison link, and three equal-width ownership cards.
- Reuse an existing phone-frame treatment if available; otherwise add one focused, token-based frame shared by all three cards.
- Link the comparison CTA to the existing pricing comparison, and each card to its matching Coin, Fractional Gold, or Vault page.

## Styling
- Add desktop-first section, card, visual, phone, typography, spacing, border, and motion styles using existing semantic tokens and the exact requested measurements.
- Keep the layout stable at narrower widths without implementing the separate responsive redesign.

## Verification
- Check the rendered section at approximately 1440px for imagery, copy, alignment, and overflow.
- Confirm the page typechecks and the preview build is clean without changing any other homepage section.

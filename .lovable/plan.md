# Build the AURUM Learn Section

## Scope
- Replace the empty `#learn` placeholder with the four requested first-time buyer cautions and three existing guide links.
- Preserve all other AURUM sections, shared navigation, price state, and editorial behavior.

## Content and presentation
- Convert the four supplied caution images to optimized WebP assets and render them decoratively with explicit dimensions.
- Build a responsive four-column caution grid that becomes two columns on tablet and one on mobile, with naturally equal-height columns and unclipped copy.
- Add the Guides list with exact category labels and published page titles; make each full row a keyboard-accessible internal link.
- Use only the existing AURUM semantic color and typography tokens, including gold-dark for all text on the light background.

## Verification
- Confirm all three guide destinations return successfully.
- Check desktop, tablet, and 390px layouts for equal-height rows, readable wrapping, tap targets, and horizontal overflow.
- Confirm the preview build remains clean.

# Final homepage waitlist CTA

## Build
- Upload the three supplied images through Lovable Assets.
- Extract the existing waitlist email validation and submission state into a shared hook, preserving the current early-access and pricing behavior.
- Replace only the empty homepage CTA with the shared site container, supplied copy, real Privacy Policy link, joined desktop form, and reusable homepage phone frames.

## Responsive behavior
- Desktop: minimum 600px section, full-bleed curtain and token gradient, copy left and two overlapping phones right inside the shared container.
- Tablet: keep a compact side-by-side composition where it remains readable, scaling the phones together.
- Mobile: stack copy, full-width form controls, then the two-phone cluster scaled as one unit.

## Verification
- Check 375, 767, 768, 834, 1023, 1024, 1280, and 1440px for alignment, overlap, and overflow.
- Verify invalid-email feedback and successful submission feedback are visible.
- Confirm build diagnostics are clean and no homepage placeholders remain.

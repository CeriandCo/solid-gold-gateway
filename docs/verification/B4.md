# B4 — Activation after the hold, code minting, delivery

Scope: activation job, delivery tick, gift-code security, email, future-redemption primitive.
Nothing published. No Stripe or email-provider key requested or required.

## Pieces

- Migration `0015_gift_card_activation_and_delivery.sql`: `commerce_settings.delivery_enabled` (default false) and `email_from` (null, email CHECK); `gift_cards.delivery_claimed_at` + code shape constraints + delivery-queue index; `gift_card_delivery_attempts` (RLS on, no policies); SECURITY DEFINER `gift_card_activate_due`, `gift_card_claim_for_delivery`, `gift_card_release_delivery_claim`, `gift_card_set_code`, `commerce_prune_delivery_attempts` (180 days), `commerce_delivery_tick` (Vault secret + tick URL).
- Schedules: `commerce-activate-due` `*/15 * * * *` (96/day), `commerce-delivery-tick` `*/5 * * * *` (288/day), `commerce-prune-delivery-attempts` `23 3 * * *`.
- `src/lib/commerce/gift-code.ts` — Crockford Base32 minting, formatting, normalisation.
- `src/lib/gift-delivery.server.ts` — HMAC hashing, email builder, Resend provider behind an interface, `runDeliveryTick`, server-only `verifyGiftCode`.
- `src/routes/api/public/gift-card-delivery-tick.ts` — shared-secret gated cron endpoint.

## Checks (20 tests, all PASS)

| # | Check | Result |
|---|-------|--------|
| 1 | Due pending card on a paid order becomes active | PASS |
| 2 | Card whose hold has not expired is unchanged | PASS |
| 3 | Due card on a review / disputed / refunded order is unchanged | PASS |
| 4 | Two concurrent activation runs activate exactly once (one ledger entry) | PASS |
| 5 | Delivery tick with no secret header → 401, no row touched | PASS |
| 6 | Delivery tick with a wrong secret → 401 | PASS |
| 7 | Delivery disabled → `awaiting_email_provider`, nothing changes | PASS |
| 8 | `email_from` null → `awaiting_email_provider` | PASS |
| 9 | No `RESEND_API_KEY` → `awaiting_email_provider` | PASS |
| 10 | Happy path: card delivered, exactly one email, correct to/from/subject | PASS |
| 11 | `code_hash` is 64 hex chars, `code_last4` set | PASS |
| 12 | The sent code appears in no column of any commerce table (both dashed and normalised forms) | PASS |
| 13 | Provider failure: card stays active, lease released, attempt logged with category | PASS |
| 14 | Next tick mints a new code; the old code no longer verifies; the new one does | PASS |
| 15 | Two concurrent ticks send exactly one email per card | PASS |
| 16 | `<script>`/`<img onerror>` gift message is escaped in the HTML part; plain text part unescaped | PASS |
| 17 | Recipient name escaped in the builder | PASS |
| 18 | 200 minted codes are 16 Crockford characters; formatting is `XXXX-XXXX-XXXX-XXXX` | PASS |
| 19 | Hashing is identical for dashed, lower-case, spaced and O/0, I/L→1 variants; wrong code → `{ found: false }` | PASS |
| 20 | `verifyGiftCode` is referenced by no route or client module and is absent from the client bundle | PASS |

Full suite: 67 tests across 5 files, all green.

## Final state

- `gift_card_orders` 0, `gift_cards` 0, `gift_card_ledger` 0, `gift_card_delivery_attempts` 0, `commerce_alerts` 0, `stripe_events` 0, `checkout_attempts` 0.
- `commerce_settings`: `checkout_enabled = false`, `delivery_enabled = false`, `currency = null`, `email_from = null`, `allowed_origins = {}`.
- Bundle scan clean: no Stripe key, no `whsec_`, no Resend key, neither pepper, no cron secret in `dist` (only the `sk_test_` prefix literal used by the live/test key check).
- Typecheck and build clean.

## Notes

- Redemption copy in the email is marked `// COPY PENDING CLIENT APPROVAL`.
- Delivery stays inert until a verified sender address and an email-provider key are in place.

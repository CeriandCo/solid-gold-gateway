# C-6 — AURUM placement in navigation

Date: 2026-09-21. Not published.

## What changed

1. **Top navigation** — no reorder needed: `siteNav` in `src/components/site-chrome.tsx` already lists `Aurum` immediately before `Learn` (positions 6 and 7 of 9), in `NAV_SECONDARY`, on both desktop and the mobile menu ("Explore" group). Verified live in the browser (link index of Aurum = 3, Learn = 4 within the desktop nav anchor set; order correct). Existing HeaderLink styling, active gold underline and hairline separators unchanged.
2. **Footer** — no change needed: the "Discover More" column already lists AURUM before Learn (footer link index 5 before 6). Verified live.
3. **Floating button** — new `src/components/aurum-floating-button.tsx`, mounted once in `src/routes/__root.tsx`:
   - Gold gradient treatment copied from the header CTA (`border-gold`, `bg-gradient-to-b from-gold-soft to-gold`, forest-deep text), 40px high, label "AURUM", `aria-label="AURUM — live gold price"`.
   - Fixed bottom right, `right-5`, bottom `calc(88px + env(safe-area-inset-bottom))` so it clears the bottom edge, the footer social row and the Intercom messenger.
   - Hidden on `/aurum`, `/aurum/*` and `/admin/*`.
   - Revealed only after the visitor scrolls past 320px, so it never covers the homepage hero's floating spot card (which occupies the same corner) or hero CTAs; stays visible for the rest of the page.
   - Entrance animation (`aurum-fab-enter`, 400ms ease-out) in `src/styles.css`, disabled under `prefers-reduced-motion: reduce`.
   - No close button (not required).
   - A `/aurum` Link needs the full search-param shape, so it passes `{ range: "1Y", note: undefined, brief: undefined, priceState: undefined }` (same defaults as the route's validateSearch).

## Browser measurements (Playwright, localhost dev build)

Nav tightness 1024–1279px (homepage, header nav right edge vs CTA left edge):

| Viewport | Nav row right edge | CTA left edge | Gap | Horizontal overflow |
|---|---|---|---|---|
| 1024 | 835px | 859px | 24px | 0 |
| 1280 | 1075px | 1099px | 24px | 0 |

Floating button (all viewports, height 844):

- 375 / 768 / 1024 / 1280 / 1440: not rendered at scroll top; appears after scrolling to y=1200; rect 80×40 at right-20px, bottom edge 88px above viewport bottom (fabBottom 756 vs footer social row top ~760 — no overlap).
- Horizontal overflow: 0 at every width.
- Footer check at full scroll: no overlap with the social row at any width.
- `/aurum` (scrolled to y=1500): button absent. Admin area excluded by path prefix.
- Nav order reads Aurum → Learn (desktop and mobile menu); footer reads AURUM → Learn.

Screenshots: /tmp/browser/c6/screenshots/ (home at 5 widths, scrolled states, /aurum).

## Gates

- `bunx tsgo --noEmit` — clean.
- `bun run test` — 17 files, 137 tests, all pass.
- `bun run build` — clean.

Nothing published.

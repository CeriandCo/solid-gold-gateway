# S-5 / S-5b — Stripe catalog sync, evidence-based

Date: 2026-09-20 (UTC). Nothing published. `commerce_settings.checkout_enabled` not changed.
No secret value printed, logged or echoed anywhere below.

## 0. Which database

Two independent readers were used and they agree:

- `psql` in the server sandbox, using the managed connection env vars `PGHOST`, `PGPORT`,
  `PGUSER`, `PGPASSWORD`, `PGDATABASE` (names only; no values shown). No connection string
  was passed on the command line.
- The Supabase read tool against the project database.

The server code itself connects via `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`
(`supabaseAdmin` in `@/integrations/supabase/client.server`) — the same project.
`select count(*) from commerce_settings` returns **1** row, so there is no second row or
second settings table (`\dt public.commerce*` lists only `commerce_alerts` and
`commerce_settings`).

## 1. Real current row (verbatim psql output)

```
 id | checkout_enabled | currency | allowed_origins | max_card_cents | delivery_enabled
----+------------------+----------+-----------------+----------------+------------------
 t  | f                |          | {}              |         200000 | f
(1 row)
```

`currency` is NULL (empty cell) and `allowed_origins` is `{}` (empty array).
`updated_at` on that row is `2026-09-20 12:15:57.990965+00`.

This does **not** match the `currency = 'usd'` + 3 allowed origins described in the S-5b
brief. Nothing in this project's codebase contains the strings `allowed_origins = 'usd'`
or `solid-gold-gateway` as a settings value. Either those values were set in a different
place/project, or they were never committed to this database. If you set them through an
admin screen, tell me which screen and I will trace the write path — the row above is what
the runtime reads today.

## 2. Sync run — RAW output, verbatim

`bun run scripts/s5-catalog-sync.ts` (imports `syncStripeCatalog()` from
`src/lib/commerce/stripe-catalog.server.ts`):

```json
{
  "mode": "test",
  "keyMode": "test",
  "currency": null,
  "mapped": 0,
  "expected": 6,
  "ready": false,
  "productId": null,
  "problems": [
    {
      "code": "no_currency"
    }
  ]
}
```

The sync bails on `no_currency` before making any Stripe call, because lookup keys are
`sqoot_gift_card_<currency>_<amount_cents>` and the currency is NULL. Nothing was written.

## 3. Stripe probe — RAW output, verbatim

`bun run scripts/s5-catalog-probe.ts`, one line per expected lookup key:

```json
{"lookup_key": "sqoot_gift_card_usd_5000", "id": "price_1UHj2yGbLxI8bO0Rkjf2rPz0", "unit_amount": 5000, "currency": "usd", "type": "one_time", "active": true, "livemode": false, "product": {"id": "prod_VIJgknGzfX7iUQ", "purpose": "sqoot_gift_card"}}
{"lookup_key": "sqoot_gift_card_usd_10000", "id": "price_1UHjGhGbLxI8bO0RWLosazoV", "unit_amount": 10000, "currency": "usd", "type": "one_time", "active": true, "livemode": false, "product": {"id": "prod_VIJgknGzfX7iUQ", "purpose": "sqoot_gift_card"}}
{"lookup_key": "sqoot_gift_card_usd_25000", "id": "price_1UHjGtGbLxI8bO0RNSCrAPKH", "unit_amount": 25000, "currency": "usd", "type": "one_time", "active": true, "livemode": false, "product": {"id": "prod_VIJgknGzfX7iUQ", "purpose": "sqoot_gift_card"}}
{"lookup_key": "sqoot_gift_card_usd_50000", "id": "price_1UHjH6GbLxI8bO0R2gNakpFW", "unit_amount": 50000, "currency": "usd", "type": "one_time", "active": true, "livemode": false, "product": {"id": "prod_VIJgknGzfX7iUQ", "purpose": "sqoot_gift_card"}}
{"lookup_key": "sqoot_gift_card_usd_100000", "id": "price_1UHjHIGbLxI8bO0RgCRyeTbY", "unit_amount": 100000, "currency": "usd", "type": "one_time", "active": true, "livemode": false, "product": {"id": "prod_VIJgknGzfX7iUQ", "purpose": "sqoot_gift_card"}}
{"lookup_key": "sqoot_gift_card_usd_200000", "id": "price_1UHjHTGbLxI8bO0RWai3m4Gk", "unit_amount": 200000, "currency": "usd", "type": "one_time", "active": true, "livemode": false, "product": {"id": "prod_VIJgknGzfX7iUQ", "purpose": "sqoot_gift_card"}}
```

Product `prod_VIJgknGzfX7iUQ` = "SQOOT Pure Gift Card", `active: true`, `livemode: false`,
`metadata.purpose = "sqoot_gift_card"`. All six prices are on that one product.

Note: this probe was run with the literal `usd` lookup keys because the database has no
currency to derive them from. The sync itself never reached Stripe.

Two other active sandbox prices exist and are irrelevant to the catalog (no lookup key, no
gift-card purpose): `price_1TXwY7GbLxI8bO0RXrVGLjDx` (50000 usd, product
`prod_UX0ZMvRsdyOpVw` "1/10 oz Gold", purpose null) and `price_1TXwUdGbLxI8bO0RRW3S1guz`
(470000 usd, product `prod_UX0V32RTn8zhi2` "1oz Gold", purpose null).

## 4. Denominations — verbatim psql output

```
                  id                  | amount_cents | active | stripe_price_id_test
--------------------------------------+--------------+--------+----------------------
 92112800-fb89-47fa-85c5-ce75a70590e1 |         5000 | t      |
 d30fa9a4-c906-4ff4-addb-a252027e517b |        10000 | t      |
 2708017f-012a-4dc0-9c58-ddbbd05e8a82 |        25000 | t      |
 4a5cd3f7-5c39-477a-992c-b7deff5aeebd |        50000 | t      |
 0da230cb-ad28-4db8-8522-18ce7b2b5e60 |       100000 | t      |
 08ea4ef4-54b6-4c2f-a3c1-80ddaa9f59a1 |       200000 | t      |
(6 rows)
```

All 6 active, all `stripe_price_id_test` empty (NULL) — the sync wrote nothing.

## 5. Buy-button conditions, with values read just now

From `runGiftCardCheckout()` in `src/lib/commerce-checkout.server.ts`:

| # | Condition | Source | Value now | Pass |
|---|---|---|---|---|
| 1 | Kill switch on | `commerce_settings.checkout_enabled` | `false` | no |
| 2 | Currency set | `commerce_settings.currency` | NULL | no |
| 3 | Stripe secret key present | `STRIPE_SECRET_KEY` env | present, mode `test` | yes |
| 4 | Request origin allowlisted | `commerce_settings.allowed_origins` | `{}` | no |
| 5 | Denomination active | `gift_card_denominations.active` | all 6 true | yes |
| 6 | Amount within cap | `max_card_cents = 200000` | largest denomination 200000 | yes |
| 7 | Mapped price id for mode `test` | `stripe_price_id_test` | NULL on all 6 | no |
| 8 | Catalog ready | `catalogStatus().ready` (0 of 6 mapped) | `false` | no |

## 6. Scope

- `checkout_enabled` untouched (`false`). Nothing published. No secret printed.
- Scripts used: `scripts/s5-catalog-sync.ts`, `scripts/s5-catalog-probe.ts`.
